import { WeblocaleDirective } from './weblocale.directive';

describe('LocaleDirective', () => {
  it('should create an instance', () => {
    const directive = new WeblocaleDirective();
    expect(directive).toBeTruthy();
  });
});
